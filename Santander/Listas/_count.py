cores = ['vermelho', 'azul', 'verde', 'azul']

print(cores.count('vermelho'))  # Conta quantas vezes 'vermelho' aparece na lista
print(cores.count('azul'))  # Conta quantas vezes 'azul' aparece na lista
print(cores.count('verde'))  # Conta quantas vezes 'verde' aparece na lista